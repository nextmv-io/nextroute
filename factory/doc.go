// © 2019-present nextmv.io inc

/*
Package factory holds functionality for creating a ready-to-go nextroute model.

You can build the model as:

	model, err := factory.NewModel(input, opts.ModelOptions)
	if err != nil {
		return err
	}
*/
package factory
